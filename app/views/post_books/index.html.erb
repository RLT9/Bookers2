<h2>Books</h2>
<table>
  <thead>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  </thead>

  <tbody>
  <% @books.each do |book| %>

<% end %>

<%= form_with model: @post_book, local:true do |f| %>
  <h1>New book</h1>
  <h4>Title</h4>
  <%= f.text_field :book_name %>
  <h4>Opinion</h4>
  <%= f.text_area :opinion %>
  <%= f.submit 'Create Book' %>
<% end %>